version: "3.8"

services:
  parksight-backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: parksight-ai-backend
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    restart: unless-stopped
